# Copy to `.env.local` (do NOT commit).
# Google OAuth Client ID for browser-based sign-in.
# Create OAuth credentials in Google Cloud Console (Authorized JS origins: e.g., http://localhost:5173)
VITE_GOOGLE_CLIENT_ID=your_google_web_client_id.apps.googleusercontent.com
